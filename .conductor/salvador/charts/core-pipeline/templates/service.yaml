apiVersion: v1
kind: Service
metadata:
  name: {{ include "core-pipeline.fullname" . }}
  labels:
    {{- include "core-pipeline.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "core-pipeline.selectorLabels" . | nindent 4 }}